import SwiftUI

enum EnergyVisualStyle {
    static let compactCornerRadius: CGFloat = 10
    static let cardCornerRadius: CGFloat = 12
    static let rowCornerRadius: CGFloat = 8

    static let computeAccent = Color(red: 0.24, green: 0.72, blue: 0.96)
    static let modelAccent = Color(red: 0.38, green: 0.56, blue: 1.00)
    static let subtleFill = Color.white.opacity(0.055)
    static let hairline = Color.white.opacity(0.17)
}
